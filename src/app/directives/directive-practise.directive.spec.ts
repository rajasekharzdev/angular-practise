import { DirectivePractiseDirective } from './directive-practise.directive';

describe('DirectivePractiseDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectivePractiseDirective();
    expect(directive).toBeTruthy();
  });
});
